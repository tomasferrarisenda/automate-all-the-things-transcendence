# apiVersion: networking.istio.io/v1beta1
# kind: Gateway
# metadata:
#   name: {{ .Values.appName }}-{{ .Values.service }}-{{ .Values.environment }}-gateway
# spec:
#   selector:
#     istio: gateway
#   servers:
#     - port:
#         number: 80
#         name: http
#         protocol: HTTP
#       hosts:
#         - {{ .Values.host }} # Istio Gateway service external IP
#       tls:
#         httpsRedirect: true
#     - port:
#         number: 443
#         name: https
#         protocol: HTTPS
#       hosts:
#         - {{ .Values.host }}
#       tls:
#         mode: SIMPLE
#         credentialName: {{ .Values.appName }}-com-crt # This should match the Certificate secretName


# If env is prod, then the value will be just <.Values.host> and not <.Values.env>.<.Values.host>
{{- $hostValue := .Values.host }}
{{- if ne .Values.env "prod" }}
{{- $hostValue = printf "%s.%s" .Values.env .Values.host }}
{{- end }}

apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: {{ .Values.appName }}-{{ .Values.service }}-{{ .Values.environment }}-gateway
spec:
  selector:
    istio: gateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - {{ $hostValue }} # Istio Gateway service external IP
      tls:
        httpsRedirect: true
    - port:
        number: 443
        name: https
        protocol: HTTPS
      hosts:
        - {{ $hostValue }}
      tls:
        mode: SIMPLE
        credentialName: {{ .Values.appName }}-com-crt # This should match the Certificate secretName


# apiVersion: networking.istio.io/v1beta1
# kind: Gateway
# metadata:
#   name: {{ .Values.appName }}-{{ .Values.service }}-{{ .Values.environment }}-gateway
# spec:
#   selector:
#     istio: gateway
#   servers:
#     - port:
#         number: 80
#         name: http
#         protocol: HTTP
#       hosts:
#       # If env is prod, then the value will be just <.Values.host> and not <.Values.env>.<.Values.host>
#         - {{ if eq .Values.env "prod" }}{{ .Values.host }}{{ else }}{{ .Values.env }}.{{ .Values.host }}{{ end }} # Istio Gateway service external IP
#       tls:
#         httpsRedirect: true
#     - port:
#         number: 443
#         name: https
#         protocol: HTTPS
#       hosts:
#         - {{ if eq .Values.env "prod" }}{{ .Values.host }}{{ else }}{{ .Values.env }}.{{ .Values.host }}{{ end }}
#       tls:
#         mode: SIMPLE
#         credentialName: {{ .Values.appName }}-com-crt # This should match the Certificate secretName







# ---

# {{- if ne .Values.environment "dev" }} # Deploy only in dev environment
# apiVersion: networking.istio.io/v1beta1
# kind: Gateway
# metadata:
#   name: {{ .Values.appName }}-{{ .Values.service }}-{{ .Values.environment }}-gateway
# spec:
#   selector:
#     istio: gateway
#   servers:
#     - port:
#         number: {{ .Values.port }}
#         name: http
#         protocol: HTTP
#       hosts:
#         - {{ .Values.host }} # Istio Gateway service external IP
# {{- end }}

